# 《再拍最后一箱！》Agent 交接文档

更新时间：2026-09-23
项目目录：`D:\ppt\失物拍卖`
当前资源版本：`20260923-17`
当前分支：`main`

本文档记录当前代码的真实完成状态、已确认设计、部署方式和接续开发注意事项。

---

## 1. 项目与部署

### GitHub

```text
https://github.com/lamb100ear/last-box-auction
```

- 默认分支：`main`
- 推送 `main` 后由 Cloudflare Pages 自动部署。
- Git 凭据使用 Windows Git Credential Manager。
- 网络偶发重置时，重试 `git push` 通常可恢复。

### Cloudflare Pages

- 项目名：`last-box-auction`
- 生产分支：`main`
- 默认地址：`https://last-box-auction.pages.dev`
- 自定义域：`https://bid.gmutaaa.com`
- SSL 已启用。

本地没有可依赖的 `gh` 或 `wrangler`，部署通过 GitHub 集成完成。

---

## 2. 技术栈与文件

- 原生 HTML
- 原生 CSS
- JavaScript ES Modules
- 无前端框架
- 本地静态服务器：`server.js`

```text
/
├─ index.html
├─ _headers
├─ .assetsignore
├─ .gitignore
├─ css/
│  └─ style.css
├─ js/
│  ├─ data.js
│  ├─ game.js
│  ├─ ui.js
│  └─ main.js
├─ server.js
├─ 再拍最后一箱-初版设计方案.md
├─ 再拍最后一箱-开发规格文档.md
├─ 再拍最后一箱-主线剧情-死者仍在出价.md
└─ AGENT_HANDOFF.md
```

职责：

- `data.js`
  - 静态配置
  - 物品和买家模板
  - 商城、规则、新闻、威胁和结局文案
  - 主线箱、证据、人物和最终选择
- `game.js`
  - 游戏状态
  - 数值规则
  - 拍卖、交易、贷款、风险、邮件和存档迁移
  - 主线状态机和结局触发
- `ui.js`
  - 桌面、窗口、页面渲染
  - 物品检视台
  - 万物通抽屉
  - 买家私聊
  - 邮件、文件夹、新闻和弹窗
- `main.js`
  - 应用入口
- `server.js`
  - 本地静态服务器

Markdown、`.git`、`.github` 和 `server.js` 由 `.assetsignore` 排除出线上站点。

---

## 3. 本地运行

```powershell
cd D:\ppt\失物拍卖
node server.js
```

默认地址：

```text
http://127.0.0.1:4173
```

语法检查：

```powershell
node --check js/data.js
node --check js/game.js
node --check js/ui.js
node --check js/main.js
```

每次修改 JavaScript 或 CSS 后，必须同步更新：

- `index.html`
- `js/main.js` 内部 import
- `js/ui.js` 内部 import
- `js/game.js` 内部 import

当前版本号为 `20260923-17`。

---

## 4. 当前完成度

当前代码已经完成：

- 阶段一：交易与反馈可信化
- 阶段二：长期新闻、威胁事件、线索基础设施和基础结局
- 图像化物品检视台
- 经济、贷款、伪造和违禁品重构
- 主线《死者仍在出价》第一版
- 四个主线结局
- 第一轮正式文档同步

当前不是原型占位状态。主线、结局、存档迁移和主要业务流程均已有可运行实现。

---

## 5. 已实现的交易与反馈

- 麻烦值和信誉值使用统一状态变化提示。
- 提示包含类型、原因、变化值和当前值。
- 任务栏状态会按升降方向闪烁。
- 状态变化写入每日结算。
- 高风险回答提高即时信任，也提高买家核验风险。
- 高风险话术不做标签或特殊颜色提示。
- 支持当场识破和成交后追回货款。
- 高风险欺骗可能升级为警察调查。
- 六名买家拥有独立语气、核验重点、问题措辞和结果反馈。
- 线上成交以微回本为主，低概率出现较大利润或亏损。
- 物品显示箱子分摊后的买入价和预计利润。
- 卖家定价越高，买家放弃概率越高。

---

## 6. 时间、贷款与世界压力

### 时间

- 每现实 1 秒推进 2.5 游戏分钟。
- 每天 `08:00` 开始。
- `00:00` 后变暗，`01:00` 强制休眠。
- 无操作 10 秒后显示右下角休眠提醒。
- 点击“立即休眠”直接结算并进入下一天。

### 贷款

- 初始现金：`800`
- 总贷款：`20000`
- 首期还款：`2000`
- 每 7 天还款一次。
- 每期金额增加 `300`。
- 应急贷款：现金 `+1000`，债务 `+1200`，每天一次。
- 日历支持主动还款和一次性还清全部贷款。
- 还清贷款进入 HE：无债一身轻。
- 连续三次逾期终止账户。

### 新闻和威胁

- 同一时间只有一条长期市场趋势。
- 趋势持续 4 到 7 天。
- 趋势只在开始、切换或结束时弹新闻。
- 日历显示趋势周期和剩余天数。
- 每局安排 4 到 5 次威胁事件。
- 威胁间隔 2 到 4 天。
- 威胁支持第一次和第二次回应。
- 事件会实际影响现金、库存、挂单、信誉和麻烦值。

---

## 7. 物品检视台

库存中的“检视”在“我的店铺”内部切换，不会创建独立物品详情窗口。

已实现：

- 基础物品、污渍、标签和伪造物图层。
- 清洁、标签检查、细节检查和补充标签。
- 使用商城购买的伪造物。
- 清洁前三次安全，第四次出现损伤和麻烦值。
- 清洁次数真实累加，第四次显示为 `4 / 3`。
- 细节检查最多两轮完成。
- 清洁、标签检查、细节检查、详细信息按键已移除。
- 补充标签和使用伪造物保留按钮入口。
- 没有材料时可直接进入商城。
- 万物通资料和商城以内嵌抽屉显示。
- 万物通类别、参考价、趋势和标签状态默认常显。
- 右侧资料栏独立滚动，左侧物品图固定。
- 检视模式自动放大店铺窗口。
- 物品使用代码生成的 16-bit 像素 SVG。
- 不同物品拥有不同轮廓。
- 收据、车票、照片和手写便条使用纸张类外观。
- 买家私聊为左右分栏：左侧只读物品信息，右侧对话。
- 左侧会按买家当前问题高亮对应事实。

---

## 8. 文件夹、邮件和线索

### 文件夹

- 分类包括全部藏品、案件线索和商城藏品。
- 线索分类会随进度显示 `?`、疑点、线索或证物。
- 线索物品强制自留，不能上架或出售。
- 周棠箱子打开后，所有物品先进入店铺库存。
- 神秘钥匙使用高亮“关键线索”库存卡，只显示“自留”按钮。
- 点击钥匙自留后，钥匙进入文件夹案件线索区。
- 线索详情原位展开在对应藏品卡片下方。
- 所有主线线索直接显示万物通调查结论，不进入普通检视台。
- 旧寄存柜收据和神秘钥匙会直接显示 `K-12` 调查结果。
- 可打开旧车站寄存柜时，档案内直接显示按钮。

### 邮件

- 初始黑匣信贷邮件默认展开。
- 后续剧情邮件默认折叠。
- 未读剧情邮件闪烁并保留红点。
- 关键时间和死亡新闻时间使用红色。
- 旧货狐狸邮件第 5 天 `22:00` 发送。
- 玩家提前休眠时，次日开盘补发并保持未读提示。
- 旧货狐狸发送邮件后退出普通买家交易池。
- 高价收购邮件在证据链完成前显示为“神秘的买家”。

---

## 9. 商城、伪造和违禁品

- 商城每天刷新六件商品。
- 分类包括服务、藏品和可伪造物品。
- 商城购买的藏品进入文件夹商城藏品分类。
- 上架时可选择一件伪造物和伪造目标。
- 目标买家会影响收益和识破概率。
- 审核型买家识破率更高。
- 违禁品使用定金加尾款。
- 成交立即获得 40% 到 60%。
- 尾款在风险窗口后到账。
- 每次出售增加永久关注度。
- 后续违禁品收益递减，调查概率递增。
- 被抓时没收尾款、追缴部分收益并增加罚金。
- 主动合法上报会增加信誉并降低麻烦。
- 违禁品第 2 到 5 次出售按递增概率触发监狱 BE。

---

## 10. 主线《死者仍在出价》

独立文档：

```text
再拍最后一箱-主线剧情-死者仍在出价.md
```

状态：第一版已实现。

### 核心流程

1. 第 4 天出现周棠私人收藏箱，未获得时后续继续出现。
2. 特殊箱使用独立外观、颜色和私人寄售条目。
3. 带回家并打开后，所有物品先进入库存。
4. 神秘钥匙只显示高亮“自留”按钮，点击后进入文件夹。
5. 第 4 天 `20:00` 收到周棠账号邮件。
6. 次日新闻公布周棠死亡，推测死亡时间为 `19:00`。
7. 旧货狐狸邮件在第 5 天 `22:00` 或次日开盘补发。
8. 旧货狐狸邮件后，旧车站寄存柜信息自动可查询并显示文件夹提醒。
9. 玩家打开寄存柜，获得旧案照片、赵衡近期照片和“他没有死”便条。
10. 证据可查看后的次日，“神秘的买家”发送高价收购邮件。
11. 接受收购直接进入“沉默的真相”结局。
12. 拒绝收购后整理证据链，进入四个最终选择。

### 主线信息表现

- `K-12` 不再是普通商品编号。
- 档案自动显示旧车站长期寄存柜结论。
- 十年前旧案新闻说明死者是周棠的老师。
- 新闻补充周棠长期调查、赵衡死亡登记和近期证据核对。
- 周棠手写便条图片直接显示“他没有死”。
- 文件夹可整理证据链。

---

## 11. 当前结局

正式结局：

| 内部 ID | 类型 | 标题 | 条件 |
| --- | --- | --- | --- |
| `debt_free` | HE | 无债一身轻 | 还清全部贷款 |
| `trouble_overload` | BE | 麻烦吞没了店铺 | 麻烦值达到 10 |
| `prison` | BE | 牢门在拍卖结束后关上 | 违禁品交易链被查获 |
| `main_police` | HE | 旧案归宗 | 公开证据并交给警方 |
| `main_zhao` | BE | 沉默的真相 | 出售全部特殊藏品 |
| `main_investigator` | NE | 调查线的无名一环 | 交给调查机构 |
| `main_blackmail` | BE | 成为交易链的一部分 | 长期威胁赵衡 |

此外还有贷款冻结、被捕等终止流程。结局界面不显示 HE、BE、NE 类型文字。

---

## 12. 用户明确偏好

- 多用中文沟通。
- 不要创建无意义的新应用或大量新窗口。
- 物品、检视、线索和买家交易尽量在现有窗口内完成。
- 不使用外部图片占位；物品图使用代码生成像素 SVG。
- 万物通是辅助系统，物品操作和线索调查是主要玩法。
- 高收益必须伴随风险。
- 新闻要长期影响市场，不要每天强制记忆新内容。
- 买家必须有独立语气和核验习惯。
- 线索不能通过普通上架出售。
- 关键提醒只在信息真正可查询时出现。
- 不要血腥猎奇，主线重点是阴谋、身份和证据链。
- 用户会逐项反馈 UI，通常需要完成代码和浏览器验证后直接汇报。

---

## 13. 存档与迁移

- `storageKey` 保存休眠检查点。
- `workingStorageKey` 保存当前工作状态。
- 载入只读取休眠检查点。
- 修改物品结构时必须更新 `normalizeState()` 和 `normalizeItemData()`。
- 新增主线字段时要为旧存档提供默认值。
- 旧货狐狸已经联系的旧存档会自动补齐寄存柜解锁和一次性提醒状态。

---

## 14. 当前已验证

- `data.js`、`game.js`、`ui.js`、`main.js` 语法检查通过。
- `git diff --check` 通过，仅存在 Windows LF/CRLF 提示。
- 桌面和移动端基础布局通过浏览器检查。
- 检视台无整体滚动，右侧栏独立滚动。
- 万物通自动资料、关键词查询和商城可操作。
- 买家私聊左右分栏正常。
- 状态提示和交易结算正常。
- 第 1 天不显示案件档案提醒。
- 第 4 天拍箱并开箱后，所有物品进入库存。
- 神秘钥匙只显示高亮“自留”，点击后进入文件夹。
- 旧货狐狸邮件后显示寄存柜可查询提醒。
- 旧寄存柜收据和钥匙档案自动显示 `K-12` 结论。
- 打开寄存柜后获得三件证据。
- 高价收购邮件显示为“神秘的买家”。
- 完整主线测试无控制台错误。

---

## 15. 当前限制

- 联系人尚未实现独立邮件分类。
- 没有音效。
- 部分普通物品图像仍是代码 SVG，不是正式位图资产。
- 主线第一版完成，但更多角色支线和额外结局仍可扩展。
- 威胁事件与最终选择的长期联动仍可继续深化。
- 小屏幕以纵向滚动兼容为主。
- 正式移动端布局尚未单独设计。

---

## 16. 后续建议

1. 增加联系人邮件分类。
2. 完善赵衡、旧货狐狸和调查机构的后续分支。
3. 扩展主线威胁事件和案件证据联动。
4. 为普通物品制作正式像素位图并保持统一风格。
5. 增加主线存档迁移和长期回归测试。
6. 扩充额外结局及结局插图。

---

## 17. 接续工作原则

1. 先读当前代码，不要只按旧设计文档判断状态。
2. 修改 JavaScript 或 CSS 后更新 `20260923-17` 形式的资源版本号。
3. 不提交或推送 GitHub，除非用户明确要求。
4. 继续使用现有窗口和系统，不新增无意义的桌面应用。
5. 完成修改后至少运行四个 JavaScript 语法检查。
6. UI 变更应使用浏览器实际验证，不只检查静态代码。
